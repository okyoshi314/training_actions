name: Welcome First-Time Contributor

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  check-first-contribution:
    runs-on: ubuntu-latest
    steps:
      - name: Check if it's the first interaction
        id: first_interaction
        uses: tj-actions/first-interaction@v1

      - name: Echo contributor name if first time
        if: steps.first_interaction.outputs.is_first_interaction == 'true'
        run: echo "🎉 Bienvenue à @${{ github.actor }} pour sa première contribution !"
