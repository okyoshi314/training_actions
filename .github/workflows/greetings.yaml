name: Welcome First-Time Contributor

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  welcome:
    runs-on: ubuntu-latest

    steps:
      - name: First interaction check
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: ''        # Laisser vide pour ne pas poster de commentaire
          pr-message: ''
        id: first

      - name: Echo contributor name if first-time
        if: steps.first.outputs.first_interaction == 'true'
        run: echo "🎉 Bienvenue à @${{ github.actor }} pour sa première contribution !"
